#include <bits/stdc++.h>

using namespace std;

vector<pair<int, int>> graph[];

int main(){
    int n, m, a, b, c, d, i;

    cin >> n >> m;
    for (i=0; i<m; i++){
        cin >> a >> b >> c >> d;
        // if (d==0){

        // }
        cout << a << b << c << d;
    }
    return 0;
}